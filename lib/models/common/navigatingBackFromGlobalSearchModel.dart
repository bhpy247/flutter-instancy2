class NavigatingBackFromGlobalSearchModel {
  final String searchString;
  final bool isSuccess;
  final int siteId;

  NavigatingBackFromGlobalSearchModel({required this.searchString, this.isSuccess = false, this.siteId = 0});
}
